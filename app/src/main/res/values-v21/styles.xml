<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="AppTheme.Base">
        <item name="android:windowContentTransitions">true</item>
        <item name="android:windowEnterTransition">@transition/transitions</item>
        <item name="android:windowExitTransition">@transition/transitions</item>
        <item name="android:windowSharedElementEnterTransition">@transition/transitions</item>
        <item name="android:windowSharedElementExitTransition">@transition/transitions</item>
    </style>

</resources>
